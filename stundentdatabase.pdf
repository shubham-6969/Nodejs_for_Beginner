===============================
🧪 SQL PRACTICE EXERCISE: STUDENT MANAGEMENT SYSTEM
===============================

📅 Date: 2025-07-16
📘 Topics Covered:
- CREATE TABLE
- INSERT INTO
- ALTER TABLE
- SELECT, UPDATE, DELETE
- CONSTRAINTS
- DROPPING TABLES

===============================
🧱 STEP 1: CREATE TABLE
===============================

CREATE TABLE students (
  student_id INT PRIMARY KEY,
  full_name VARCHAR(100) NOT NULL,
  email VARCHAR(100) UNIQUE,
  age INT CHECK (age >= 18),
  department VARCHAR(50),
  enrollment_date DATE DEFAULT CURRENT_DATE
);

===============================
✍️ STEP 2: INSERT SAMPLE DATA
===============================

INSERT INTO students (student_id, full_name, email, age, department)
VALUES 
(1, 'Aman Sharma', 'aman@example.com', 20, 'Computer Science'),
(2, 'Riya Mehta', 'riya@example.com', 22, 'Mathematics'),
(3, 'Kunal Singh', 'kunal@example.com', 19, 'Physics');

===============================
✏️ STEP 3: ALTER TABLE
===============================

-- Add new column
ALTER TABLE students ADD COLUMN phone VARCHAR(15);

-- Rename column (MySQL 8.0+)
ALTER TABLE students RENAME COLUMN full_name TO name;

-- For older MySQL:
-- ALTER TABLE students CHANGE full_name name VARCHAR(100);

===============================
🔍 STEP 4: RUN SELECT/UPDATE/DELETE QUERIES
===============================

-- Select all students
SELECT * FROM students;

-- Select names and emails
SELECT name, email FROM students;

-- Find students older than 20
SELECT * FROM students WHERE age > 20;

-- Update department for student_id 2
UPDATE students SET department = 'Data Science' WHERE student_id = 2;

-- Delete student with ID 3
DELETE FROM students WHERE student_id = 3;

===============================
🧼 STEP 5: DROP THE TABLE (OPTIONAL)
===============================

DROP TABLE students;

===============================
✅ TASK CHECKLIST
===============================

| Task                              | Done? |
|-----------------------------------|--------|
| Create table with constraints     |        |
| Insert at least 3 rows            |        |
| Alter table (add & rename column) |        |
| Write SELECT, UPDATE, DELETE      |        |
| Drop table                        |        |

🧠 Tip: Practice running each query in your SQL editor (e.g., MySQL Workbench, db-fiddle.com, or VS Code extensions)

===============================
👏 Great Job Practicing SQL!
===============================
